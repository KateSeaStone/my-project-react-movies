import PropTypes from 'prop-types';

const Footer = (props) => {
  //const { handleClick } = props;
  return (
    <footer>©  2020 | All Rights Reserved | Developed with
      <a href="#"> by GoIT Students</a>
    </footer>
  )
};


// Footer.propTypes = {

// };

export default Footer;